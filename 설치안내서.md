# 학생 상담 시스템 — GitHub 홈페이지 설치 안내서

GitHub도 코딩도 처음이신 분을 기준으로 썼습니다.
**순서대로만 따라 하시면 됩니다.** 어려운 이해는 필요 없습니다.

---

## 전체 그림 (1분만 읽어주세요)

```
   선생님 ──▶  GitHub 홈페이지  ──▶  Apps Script 서버  ──▶  구글 시트
              (화면만 담당)         (비밀번호 확인,        (진짜 데이터)
                                     데이터 꺼내기)
```

- **GitHub에 올리는 파일은 전 세계 누구나 볼 수 있습니다.** 그래서 여기엔 화면 코드만 둡니다.
- **비밀번호 확인과 시트 조회는 전부 Apps Script(서버)에서 합니다.**
  시트 주소도, 비밀번호도, 다른 반 데이터도 브라우저로 절대 나가지 않습니다.

---

## 준비물

- 구글 계정 (지금 쓰시는 것)
- 시간 약 30분
- 파일 2개: `Code.gs`, `index.html`

---

# 1단계 — Apps Script 서버 만들기 (약 10분)

### 1-1. 스크립트 열기

1. 성적 구글 시트를 엽니다.
2. 상단 메뉴 **확장 프로그램 → Apps Script** 클릭

> ⚠️ **중요**: 반드시 시트에서 이 경로로 열어야 합니다.
> (그래야 나중에 시트 상단에 관리 메뉴가 생깁니다.)

### 1-2. 코드 붙여넣기

1. 왼쪽에 `Code.gs` 파일이 보입니다. 클릭합니다.
2. **기존 내용을 전부 지웁니다.** (Ctrl+A → Delete)
3. 제가 드린 `Code.gs` 내용을 **전부 복사해서 붙여넣습니다.**
4. `student_counseling.html` 파일이 왼쪽 목록에 있다면, 옆의 점 3개(⋮) → **삭제**
   (이제 화면은 GitHub이 담당하므로 필요 없습니다.)
5. 💾 **저장** (Ctrl+S)

### 1-3. 첫 실행 & 권한 승인

1. 상단 가운데 함수 선택 상자에서 **`초기설정`** 을 고릅니다.
2. **▶ 실행** 클릭
3. "권한 필요" 창이 뜹니다 → **권한 검토** 클릭
4. 본인 구글 계정 선택
5. "Google에서 확인하지 않은 앱입니다" 경고가 나옵니다
   → 왼쪽 아래 **고급** → **(프로젝트 이름)(으)로 이동** 클릭
   → **허용**

> 이 경고는 정상입니다. 본인이 만든 스크립트라 구글이 아직 심사하지 않았다는 뜻일 뿐입니다.

6. 실행이 끝나면 화면 아래 **실행 로그**에 이런 내용이 나옵니다:

```
'계정' 시트를 만들고 관리자 계정을 발급했습니다.
    aB3kR9mP
⚠️ 이 비밀번호는 지금만 볼 수 있습니다. 반드시 메모해 두세요.
```

### ⚠️ 이 비밀번호를 지금 메모장에 적어두세요. 다시 볼 수 없습니다.

(잊어버려도 괜찮습니다. 나중에 재발급할 수 있습니다. 아래 4단계 참고)

### 1-4. 웹 앱으로 배포하기

1. 오른쪽 위 파란 **배포** 버튼 → **새 배포**
2. 왼쪽 위 톱니바퀴(⚙️) → **웹 앱** 선택
3. 아래처럼 설정합니다:

| 항목 | 설정값 |
|---|---|
| 설명 | 상담시스템 v1 (아무거나) |
| 다음 사용자 인증 정보로 실행 | **나 (본인 이메일)** |
| 액세스 권한이 있는 사용자 | **모든 사용자** |

> 🔎 "모든 사용자"가 불안하실 수 있는데, 괜찮습니다.
> 누구나 서버에 **말을 걸 수는** 있지만, **비밀번호가 없으면 아무 데이터도 받지 못합니다.**
> 이 설정을 "나만"으로 하면 다른 선생님들이 접속할 수 없게 됩니다.

4. **배포** 클릭
5. 나오는 **웹 앱 URL을 복사**합니다. 이렇게 생겼습니다:

```
https://script.google.com/macros/s/AKfycbx.....매우긴글자...../exec
```

### 📌 이 주소를 메모장에 붙여넣어 두세요. 2단계에서 씁니다.

---

# 2단계 — index.html에 주소 넣기 (1분)

1. `index.html` 파일을 메모장(또는 VS Code)으로 엽니다.
2. 위에서 **20번째 줄쯤**에 이런 부분이 있습니다:

```javascript
const API_URL = '여기에_웹앱_주소를_붙여넣으세요';
```

3. 따옴표 안을 방금 복사한 주소로 바꿉니다:

```javascript
const API_URL = 'https://script.google.com/macros/s/AKfycbx...../exec';
```

4. 저장합니다.

> ⚠️ 따옴표 `'` 는 지우지 마세요. 주소만 바꿉니다.

---

# 3단계 — GitHub에 홈페이지 올리기 (약 15분)

### 3-1. GitHub 가입

1. https://github.com 접속
2. **Sign up** 클릭
3. 이메일 / 비밀번호 / 사용자 이름(영문) 입력
   - 사용자 이름은 주소에 들어갑니다. 예: `namju-teacher`
4. 이메일 인증 완료

### 3-2. 저장소(Repository) 만들기

1. 오른쪽 위 **＋** → **New repository**
2. 설정:

| 항목 | 값 |
|---|---|
| Repository name | `counseling` (원하는 영문 이름) |
| Public / Private | **Public** ← 무료 계정은 이걸로 해야 홈페이지가 열립니다 |
| Add a README file | 체크 안 함 |

3. **Create repository** 클릭

> 🔎 Public이면 코드가 공개되는데 괜찮나요? → **네, 괜찮습니다.**
> `index.html`에는 화면 코드와 서버 주소밖에 없습니다.
> 비밀번호도, 시트 주소도, 학생 데이터도 들어있지 않습니다.

### 3-3. 파일 올리기

1. 만들어진 페이지에서 **uploading an existing file** 링크 클릭
   (또는 **Add file → Upload files**)
2. `index.html` 파일을 끌어다 놓습니다.
3. 아래 **Commit changes** 초록 버튼 클릭

### 3-4. 홈페이지 켜기 (GitHub Pages)

1. 저장소 위쪽 **Settings** (톱니바퀴) 클릭
2. 왼쪽 목록에서 **Pages** 클릭
3. **Branch** 항목에서:
   - 왼쪽 상자: **main** 선택
   - 오른쪽 상자: **/ (root)** 선택
   - **Save** 클릭
4. 1~2분 기다린 뒤 새로고침하면 위쪽에 주소가 나옵니다:

```
https://내아이디.github.io/counseling/
```

### 🎉 이 주소가 완성된 홈페이지입니다.

---

# 4단계 — 계정 승인하기 (관리자 = 선생님)

시트를 새로고침하면 상단에 **🔐 상담시스템 관리** 메뉴가 생겨 있습니다.
(안 보이면 시트 탭을 닫았다 다시 열어보세요.)

### 다른 선생님이 신청하면

1. 그 선생님이 홈페이지에서 **계정 신청하기** → 이름·반 입력 → 신청
2. 구글 시트 **`계정`** 탭에 `대기` 상태로 한 줄이 생깁니다
3. 선생님은 시트에서 **그 줄을 클릭**
4. 상단 메뉴 **🔐 상담시스템 관리 → ② 선택한 줄 승인하기**
5. 비밀번호가 화면에 뜹니다 → **메모해서 그 선생님께 직접 전달**

> 여러 명을 한 번에 승인하려면 **③ 대기 중인 신청 모두 승인하기**

### 비밀번호를 잊었다는 연락이 오면

해당 줄 클릭 → **④ 선택한 줄 비밀번호 재발급**
(새 비밀번호가 나오고, 기존 비밀번호는 즉시 못 쓰게 됩니다)

### 전근 등으로 접속을 막고 싶으면

해당 줄 클릭 → **⑤ 선택한 줄 사용 정지 / 해제**

---

# 완성된 시스템의 동작

| 상황 | 결과 |
|---|---|
| 비밀번호 없이 홈페이지 접속 | 로그인 화면만 보임. 데이터 0개 |
| 승인 안 된 사람이 로그인 시도 | "아직 승인되지 않은 계정입니다" |
| 3반 담임이 로그인 | **바로 1학년 3반 학생 명단이 뜸** |
| 로그인 후 다른 반 선택 | 왼쪽 드롭다운으로 전체 반 조회 가능 |
| 12시간 지남 | 자동 로그아웃 → 다시 비밀번호 입력 |

---

# 5단계 — 내신 평균 · 9등급 환산 (v2 추가 기능)

## 이미 설치하신 뒤에 파일을 새로 받으셨다면

두 파일 모두 갈아끼우고 **다시 배포**해야 반영됩니다.

1. Apps Script에서 `Code.gs` 내용 전체를 새 파일 내용으로 교체 → 저장
2. **배포 → 배포 관리 → 연필(✏️) → 버전: 새 버전 → 배포**
   (주소는 그대로 유지됩니다)
3. GitHub 저장소에서 `index.html` 클릭 → 연필(✏️) → 내용 전체 교체 → Commit changes
   - **주의**: 새 `index.html`의 `API_URL`에 웹 앱 주소를 다시 넣어야 합니다

## 무엇이 달라지나요

### ① 9등급 병기

등급 칸이 **`2 → 3`** 형태로 바뀝니다. 왼쪽이 현행 5등급, 오른쪽이 9등급 환산값입니다.
1차등급·2차등급·최종등급 모두 적용됩니다.

### ② 내신 평균

- 학생 카드에 `📊 평균 2.17 → 3.42 등급`
- 상담 화면 내신 표 위에 `5등급제 평균 / 9등급제 평균` 배지
- 내신 표 맨 아랫줄에 **평균** 행 (환산총점 평균 + 등급 평균 + 전교 석차백분율 평균)

성적이 입력된 과목만 계산에 넣습니다. 아직 안 본 과목은 자동으로 빠집니다.

## 9등급은 어떻게 계산되나요

**시트의 수식은 전혀 건드리지 않습니다.** 화면에서만 다시 계산합니다.

```
내신성적 시트의 전교생 점수를 모두 읽음
        ↓
과목별로 석차백분율 다시 계산
   석차백분율 = (석차 + (동석차 - 1) ÷ 2) ÷ 인원수 × 100
        ↓
9등급제 기준 적용
   1등급 4%  / 2등급 11% / 3등급 23% / 4등급 40% / 5등급 60%
   6등급 77% / 7등급 89% / 8등급 96% / 그 이상 9등급
```

기준이 되는 점수는 선생님 시트 규칙과 똑같이 맞췄습니다.

| 등급 | 기준 점수 |
|---|---|
| 1차등급 | 1차 시험 점수 |
| 2차등급 | 2차 시험 점수 |
| 최종등급 | 환산총점 (수행평가 포함) |

동점자 처리도 시트에 적어두신 공식 그대로입니다.

> ⚠️ **한 가지 전제**
> 9등급은 **`내신성적` 시트에 들어 있는 학생 전체**를 모집단으로 계산합니다.
> 지금처럼 1학년 191명이 모두 들어 있으면 전교 기준으로 정확합니다.
> 만약 시트에 일부 반만 넣어두시면 그 인원 안에서의 등급이 되니 주의해 주세요.

> ℹ️ **평균 등급에 단위수 가중치는 넣지 않았습니다.**
> 12과목을 단순 평균합니다. 단위수 반영이 필요하시면 알려주세요.

---

# ❓ 문제가 생기면

### "명단을 불러오지 못했습니다"가 뜰 때

1. `index.html`의 `API_URL`이 제대로 들어갔는지 확인
2. 웹 앱 배포 설정이 **"모든 사용자"** 인지 확인
3. 시트 탭 이름 확인: 시트 메뉴 **🔐 상담시스템 관리 → ⑥ 시트 연결 상태 점검**

### 로그인 버튼을 눌러도 반응이 없을 때

브라우저에서 **F12** → **Console** 탭에 빨간 글씨가 있는지 확인해 주세요.
그 내용을 알려주시면 원인을 찾아드릴 수 있습니다.

### 코드를 고친 뒤 반영이 안 될 때

Apps Script는 **저장만으로는 반영되지 않습니다.**
**배포 → 배포 관리 → 연필(✏️) → 버전: 새 버전 → 배포** 를 눌러야 합니다.
(이때 URL은 바뀌지 않습니다.)

### GitHub 파일을 수정하고 싶을 때

저장소에서 `index.html` 클릭 → 오른쪽 연필(✏️) 아이콘 → 수정 → **Commit changes**
1~2분 뒤 홈페이지에 반영됩니다.

---

# 🔒 보안 관련 당부

- 비밀번호는 **카카오톡 단체방 말고 개별 전달**을 권합니다.
- 학기가 바뀌면 **⑤ 사용 정지**로 이전 담임 계정을 정리해 주세요.
- `계정` 시트의 **비밀번호(암호화)** 열은 절대 직접 수정하지 마세요.
  (복잡한 글자로 보이는 게 정상입니다. 원래 비밀번호는 복원할 수 없습니다.)
- 학생 개인정보가 담긴 시스템이므로, 학교 개인정보 처리 방침을 한 번 확인해 보시길 권합니다.
