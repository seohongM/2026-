# 학생 상담 시스템 — 처음부터 끝까지 따라 하기

**점검 결과: 코드에는 문제가 없습니다.**
(중복 함수 없음 / 구버전 런타임 충돌 없음 / 기능 테스트 32항목 통과)

작동하지 않는 원인은 거의 **Apps Script 배포 설정**입니다.
이번 파일에는 **어디서 막혔는지 짚어주는 진단 기능**을 넣었습니다.

> 이미 하다 마셨어도 괜찮습니다. **1단계부터 그대로 다시** 하시면 됩니다.
> 중복 실행해도 데이터가 망가지지 않습니다.

---

# 준비

- [ ] 파일 2개를 내려받아 둡니다 → `Code.gs`, `index.html`
- [ ] `index.html`은 **메모장**으로 열 수 있게 준비합니다
  (윈도우: 파일 우클릭 → 연결 프로그램 → 메모장)

---

# 1부. Apps Script 서버 만들기

### 1. 스크립트 편집기 열기
구글 시트를 열고 → 상단 **확장 프로그램** → **Apps Script**

> 반드시 이 경로로 여세요. 따로 만들면 시트에 관리 메뉴가 안 생깁니다.

### 2. 기존 코드 지우기
왼쪽에서 `Code.gs` 클릭 → 편집창 안을 클릭 → **Ctrl+A** → **Delete**

### 3. 새 코드 붙여넣기
드린 `Code.gs` 내용을 **전부** 복사해서 붙여넣기 → **Ctrl+S** (저장)

### 4. 옛날 화면 파일 지우기
왼쪽 목록에 `student_counseling.html`이 있으면
옆 점 3개(⋮) → **삭제**

### 5. 첫 실행
상단 가운데 함수 선택 상자에서 **`초기설정`** 선택 → **▶ 실행**

### 6. 권한 승인
- **권한 검토** → 본인 계정 선택
- "확인되지 않은 앱" 경고 → 왼쪽 아래 **고급** → **(프로젝트 이름)(으)로 이동** → **허용**

> 이 경고는 정상입니다. 본인이 만든 스크립트라는 뜻입니다.

### 7. 🔑 관리자 비밀번호 받아 적기
아래 **실행 로그**에 8글자 비밀번호가 나옵니다.

```
'계정' 시트를 만들고 관리자 계정을 발급했습니다.
    aB3kR9mP     ← 이것
```

- [ ] **메모장에 적어두세요. 다시 볼 수 없습니다.**

> 로그가 안 보이면 상단 **실행 기록** 탭을 눌러보세요.
> 이미 관리자가 있다는 메시지가 나오면 그대로 다음 단계로 가시고,
> 비밀번호를 모르시면 → 아래 **[3부]** 를 보세요.

---

# 2부. 배포하기 ★ 여기가 가장 중요합니다 ★

### 8. 배포 시작
오른쪽 위 파란 **배포** → **새 배포**

> ⚠️ **[새 배포]는 지금 이 한 번만 누릅니다.**
> 나중에 코드를 고칠 때 또 누르면 **주소가 새로 생겨서** GitHub 파일까지
> 전부 다시 고쳐야 합니다. 수정 방법은 아래 **[7부]** 를 보세요.

### 9. 유형 고르기
왼쪽 위 **톱니바퀴(⚙️)** → **웹 앱**

### 10. 설정값 맞추기

| 항목 | 반드시 이 값 |
|---|---|
| 다음 사용자 인증 정보로 실행 | **나** (본인 이메일) |
| 액세스 권한이 있는 사용자 | **모든 사용자** |

> ⚠️ **"모든 사용자"가 아니면 100% 작동하지 않습니다.**
> "Google 계정이 있는 모든 사용자"도 안 됩니다. 반드시 **"모든 사용자"**.
> 비밀번호가 따로 있으니 안전합니다.

### 11. 배포 → 주소 복사
**배포** 버튼 → 나오는 **웹 앱 URL** 복사

```
https://script.google.com/macros/s/AKfycb....../exec
```

- [ ] 메모장에 붙여넣어 두세요.
- [ ] **끝이 `/exec` 인지 확인**하세요. `/dev` 로 끝나면 안 됩니다.

### 12. 주소가 살아있는지 확인
복사한 주소를 **브라우저 주소창에 그대로 붙여넣고 접속**해 보세요.

```
{"ok":true,"data":{"message":"상담 시스템 서버가 정상 작동 중입니다."}}
```

- 이런 글자가 보이면 ✅ **성공**
- 로그인 화면이 나오면 ❌ → **10번으로 돌아가 "모든 사용자"로 고치고 다시 배포**

---

# 3부. 화면 파일 준비

### 13. index.html 열기
`index.html`을 **메모장**으로 엽니다.

### 14. 주소 넣기
위에서 **19번째 줄** 근처를 찾습니다.

```javascript
const API_URL = '여기에_웹앱_주소를_붙여넣으세요';
```

따옴표 안만 바꿉니다.

```javascript
const API_URL = 'https://script.google.com/macros/s/AKfycb....../exec';
```

- [ ] 작은따옴표 `'` 두 개는 지우지 않기
- [ ] 세미콜론 `;` 지우지 않기

### 15. 저장
**Ctrl+S**

---

# 4부. GitHub에 올리기

### 16. 가입
https://github.com → **Sign up** → 이메일·비밀번호·아이디(영문) → 이메일 인증

### 17. 저장소 만들기
오른쪽 위 **＋** → **New repository**

| 항목 | 값 |
|---|---|
| Repository name | `counseling` |
| 공개 여부 | **Public** |
| Add a README | 체크 안 함 |

**Create repository** 클릭

> Public이어야 무료로 홈페이지가 열립니다.
> `index.html`에는 비밀번호도 시트 주소도 없으니 안전합니다.

### 18. 파일 올리기
**uploading an existing file** 클릭 → `index.html` 끌어다 놓기 → 아래 **Commit changes**

### 19. 홈페이지 켜기
**Settings**(톱니바퀴) → 왼쪽 **Pages** →
Branch: **main** / **/ (root)** → **Save**

### 20. 주소 확인
1~2분 뒤 새로고침하면 주소가 나옵니다.

```
https://내아이디.github.io/counseling/
```

---

# 5부. 접속 확인

### 21. 홈페이지 열기
위 주소로 접속 → 로그인 화면이 뜹니다.

### 22. ⭐ 연결 상태 확인 (문제가 있으면 여기서 다 나옵니다)
로그인 화면 오른쪽 아래 **🔧 연결 상태 확인** 버튼을 누릅니다.

**✅ 이렇게 나오면 정상입니다**

```
✅ [1] 주소 형식 정상
✅ [2] 서버 연결(기본) 정상
✅ [3] 서버 연결(예비) 정상
```

**❌ 문제가 있으면 원인과 해결법을 직접 알려줍니다**

| 나오는 메시지 | 해야 할 일 |
|---|---|
| `[1] API_URL이 비어 있습니다` | 14번 단계로 → 주소 넣기 |
| `[1] 주소가 /dev 로 끝납니다` | **배포 → 배포 관리**에 있는 `/exec` 주소를 복사해 14번에 넣기 |
| `[2][3] 실패` | 10번 → **액세스: 모든 사용자** 로 고치고 다시 배포 |
| `구글 로그인 페이지가 돌아왔습니다` | 10번 → 같은 문제 |

### 23. 로그인
7번에서 적어둔 **관리자 비밀번호** 입력 → 학생 명단이 뜨면 **완성**입니다. 🎉

---

# 6부. 다른 선생님 계정 만들기

### 24. 신청받기
그 선생님이 홈페이지에서 **계정 신청하기** → 이름·반 입력 → 신청

### 25. 승인하기
구글 시트를 새로고침하면 상단에 **🔐 상담시스템 관리** 메뉴가 생깁니다.

1. `계정` 탭으로 이동
2. 승인할 사람의 **줄을 클릭**
3. **🔐 상담시스템 관리 → ② 선택한 줄 승인하기**
4. 뜨는 비밀번호를 메모해서 **본인에게 직접 전달**

| 필요할 때 | 메뉴 |
|---|---|
| 여러 명 한꺼번에 승인 | ③ 대기 중인 신청 모두 승인하기 |
| 비밀번호를 잊었다고 할 때 | ④ 선택한 줄 비밀번호 재발급 |
| 전근·퇴직으로 막을 때 | ⑤ 선택한 줄 사용 정지 / 해제 |
| 관리자 비밀번호를 잊었을 때 | `계정` 시트에서 **관리자** 줄 클릭 → ④ 재발급 |

---

# 7부. 나중에 수정할 때 (주소는 절대 바뀌지 않습니다)

## 🚫 절대 하지 말 것

> **[배포] → [새 배포] 를 다시 누르지 마세요.**
> 누르는 순간 **주소가 새로 생깁니다.** 그러면 `index.html`도 고치고
> GitHub에도 다시 올려야 하는 일이 반복됩니다.

## ✅ 무엇을 고쳤느냐에 따라

| 고친 것 | 해야 할 일 | 주소 변경 |
|---|---|---|
| `Code.gs` (서버) | **배포 → 배포 관리 → 연필(✏️) → 버전: `새 버전` → 배포** | 없음 |
| `index.html` (화면) | GitHub에서 파일 교체 후 **Commit changes** | 없음 |
| 시트의 성적·학생 데이터 | 아무것도 안 함 (새로고침하면 바로 반영) | 없음 |
| 계정 승인 · 비밀번호 발급 | 아무것도 안 함 | 없음 |

평소 성적 입력이나 상담 저장에는 **배포가 전혀 필요 없습니다.**
`Code.gs`를 고쳤을 때만 위 첫 줄을 하시면 됩니다.

## Code.gs 수정 순서 (그림으로)

```
Apps Script 편집기에서 코드 교체 → Ctrl+S 저장
        ↓
오른쪽 위 [배포] → [배포 관리]
        ↓
기존 배포 오른쪽 위 연필(✏️) 클릭
        ↓
'버전' 드롭다운 → 맨 위 [새 버전] 선택
        ↓
[배포] 클릭  →  끝. 주소는 그대로입니다.
```

## index.html 수정 순서

```
GitHub 저장소 → index.html 클릭 → 오른쪽 연필(✏️)
        ↓
Ctrl+A 로 전체 선택 → 새 내용 붙여넣기
        ↓
API_URL 줄에 기존 주소가 들어있는지 확인   ← 이것만 주의
        ↓
아래 [Commit changes] → 1~2분 뒤 반영
```

> 새 `index.html`을 받으실 때마다 **API_URL 줄만** 다시 채우시면 됩니다.
> 주소는 늘 같으니 메모장에 적어두시면 편합니다.

## 왜 저장만으로는 반영되지 않나

Apps Script는 **편집 중인 코드**와 **배포된 코드**를 따로 둡니다.
사진을 찍어두고 그 사진을 사람들에게 보여주는 것과 같습니다.
저장은 원본을 고친 것이고, 사람들이 보는 사진은 **'새 버전'을 눌러야** 다시 찍힙니다.

---

# ❗ 그래도 안 될 때

### 순서대로 딱 3가지만 확인하세요

**① 시트 쪽 점검**
Apps Script 편집기 → 함수 목록에서 **`연결테스트`** 선택 → **▶ 실행** → 실행 로그 확인
(또는 시트 메뉴 **🔐 상담시스템 관리 → ⑦ 전체 점검**)

시트 연결·계정·학생 수·서버 응답을 한 번에 보여줍니다.

**② 홈페이지 쪽 점검**
로그인 화면의 **🔧 연결 상태 확인** 버튼

**③ 코드를 고쳤는데 반영이 안 될 때**
Apps Script는 **저장만으로는 반영되지 않습니다.**

> **배포 → 배포 관리 → 연필(✏️) → 버전: `새 버전` → 배포**

이걸 안 누르면 옛날 코드가 계속 돕니다. **가장 흔한 원인입니다.**
(주소는 바뀌지 않습니다.)

### 그래도 안 되면

위 ①②의 **결과 화면을 그대로 복사해서** 알려주세요.
어디가 막혔는지 정확히 짚어드리겠습니다.

---

# 참고 — 이번 버전의 기능

### 로그인
- 비밀번호 하나로 로그인 → 본인 반이 바로 표시
- 왼쪽 드롭다운으로 다른 반도 조회 가능
- 12시간 후 자동 로그아웃
- 비밀번호는 시트에 **되돌릴 수 없는 형태**로 저장 (시트를 봐도 알 수 없음)

### 9등급 환산
등급 칸이 **`2 → 3`** (5등급 → 9등급)으로 표시됩니다.
시트 수식은 건드리지 않고 화면에서만 계산합니다.

```
내신성적 시트의 전교생 점수 → 과목별 석차백분율 다시 계산
   석차백분율 = (석차 + (동석차 - 1) ÷ 2) ÷ 인원수 × 100
→ 9등급 기준 적용
   1등급 4% / 2등급 11% / 3등급 23% / 4등급 40% / 5등급 60%
   6등급 77% / 7등급 89% / 8등급 96% / 그 이상 9등급
```

| 등급 | 기준 점수 |
|---|---|
| 1차등급 | 1차 시험 점수 |
| 2차등급 | 2차 시험 점수 |
| 최종등급 | 환산총점 (수행 포함) |

### 내신 평균
- 학생 카드 → `📊 평균 2.17 → 3.42 등급`
- 내신 표 위 배지 → `5등급제 평균 / 9등급제 평균`
- 내신 표 맨 아랫줄 → 환산총점 평균 · 등급 평균 · 전교 석차백분율 평균

성적이 없는 과목은 자동으로 빠집니다. 단위수 가중치는 넣지 않았습니다(단순 평균).

> 9등급은 `내신성적` 시트에 든 학생 **전원**을 모집단으로 잡습니다.
> 191명이 모두 들어 있으면 전교 기준으로 정확합니다.
