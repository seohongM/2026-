# 학생 상담 관리 시스템 — 작업 지침

이 폴더는 **고등학교 담임 선생님용 학생 상담·성적 관리 사이트**입니다.
아래 규칙과 구조를 먼저 읽고 작업해 주세요.

---

## 사용자에 대해

- 고등학교 교사입니다. **GitHub·코딩·AI를 처음 다룹니다.**
- 전문 용어보다 **어디를 눌러야 하는지** 를 알려드려야 합니다.
- 파일이 여러 벌로 늘어나면 혼란스러워하십니다. **항상 이 폴더의 파일 2개만** 유지하세요.
- 한국어로 대화합니다.

---

## 🚫 절대 규칙

### 1. 주소가 바뀌는 방법은 어떤 경우에도 안내하지 않는다

- **[새 배포]** 를 누르라고 하지 말 것 (배포 ID가 새로 생겨 주소가 바뀝니다)
- **새 GitHub 저장소**를 만들라고 하지 말 것
- 배포 목록이 비어 있는 등 예외 상황이면 → **"거기서 멈추고 알려달라"** 고 안내할 것

Apps Script 코드를 고쳤을 때의 유일한 배포 방법:

```
배포 → 배포 관리 → 기존 배포의 연필(✏️) → 버전: [새 버전] → 배포
```

### 2. 구글 시트를 공개하지 않는다

참고 사이트 중에는 시트를 `pub?output=csv` 로 게시해 읽는 것이 있으나,
그러면 주소만 알면 누구나 전교생 성적을 봅니다. **이 방식은 쓰지 않습니다.**
데이터는 반드시 로그인된 Apps Script API를 통해서만 나갑니다.

### 3. 시트 수식은 건드리지 않는다

내신 등급·환산총점·성취도는 모두 시트 수식입니다. 화면에서만 가공합니다.

---

## 구조

```
https://seohongm.github.io/2026-/        ← GitHub Pages (저장소 이름: 2026-)
        │  index.html — 화면만. 시트 주소도 비밀번호도 없음
        │
        │  POST (Content-Type: text/plain) + JSONP 예비통로
        ▼
https://script.google.com/a/macros/<학교도메인>/s/<ID>/exec
        │  Code.gs — 비밀번호 확인 · 시트 조회 · 통계 계산
        ▼
구글 시트 (비공개)
```

- 학교 구글 계정(Workspace)이라 웹 앱 주소가 `/a/macros/<도메인>/s/.../exec` 형태입니다. **정상입니다.**
- 배포 설정: 실행 = 나 / 액세스 = **모든 사용자** (비밀번호 화면이 앞을 막고 있어 안전)

### 파일

| 파일 | 어디에 붙여넣나 | 고쳤을 때 |
|---|---|---|
| `Code.gs` | Apps Script의 Code.gs | Ctrl+S → 배포 관리 → 연필 → 새 버전 → 배포 |
| `index.html` | GitHub 저장소 `2026-` 의 index.html | Commit changes (배포 불필요) |

> `index.html` 22번째 줄 `API_URL` 에 웹 앱 주소를 넣어야 동작합니다.
> 새 index.html을 드릴 때마다 이 줄을 다시 채워야 한다고 안내하세요.

---

## 구글 시트 구조 (매우 중요)

스프레드시트 ID: `1SUzjzlI4KxJrpFMPbPeXTsZOPmH9MeniXDp7h04Zrz4`
2026학년도 1학년 · 전교 191명 · 7개 반

### 탭 목록

```
설명 / 내신_반영비율 / 내신_성취도분할점수 / 학생 목표 / 내신성적 /
내신_등급계산 / 상담_계산 / 상담_2차목표 /
모의고사_3월 / 모의고사_6월 / 모의고사_9월 / 모의고사_10월 / 상담내용 /
계정 ← 이 스크립트가 자동 생성
```

### 내신성적 (가장 조심할 곳)

```
A열 = 반 / B열 = 번호 / C열 = 이름
D열(index 3)부터 과목당 11칸씩 12과목 반복

  +0 1차시험   +1 1차등급   +2 2차시험   +3 2차등급
  +4 1차수행   +5 2차수행   +6 환산총점  +7 최종등급
  +8 1차성취도 +9 2차성취도 +10 최종성취도

SUBJECTS start = 3, 14, 25, 36, 47, 58, 69, 80, 91, 102, 113, 124
과목명 = 공통국어1·2, 공통수학1·2, 공통영어1·2, 통합사회1·2, 통합과학1·2, 한국사1·2
        (뒤의 1=1학기, 2=2학기)
```

**열을 중간에 끼워 넣으면 그 뒤 과목이 전부 밀립니다.** 구조 변경 시 `SUBJECTS` 재계산 필요.

### 상담_2차목표

```
머리글 2줄. A반 B번호 C이름.
D열(index 3)부터 과목당 3칸 × 12과목
  +0 1차점수  +1 1차등급  +2 +1등급필요
```

⚠️ **이 구조는 아직 실물로 확인되지 않았습니다.** 다른 사이트 코드에서 유추한 것입니다.
문제가 생기면 사용자에게 `check상담목표시트` 함수를 실행해 로그를 보여달라고 요청하세요.

### 모의고사_3/6/9/10월

머리글 2줄(1행 과목명 병합 / 2행 원점수·표준점수·백분위·등급). `analyzeMockSheet_` 가 자동 인식합니다.
**영어·한국사는 절대평가라 백분위 열이 없습니다.**

### 상담내용

A반 B번호 C이름, **D열부터** 상담 기록이 `[날짜]\n내용` 형태로 오른쪽으로 쌓입니다.

### 학생 목표

희망 대학 · 희망 학과. `analyzeTargetSheet_` 가 머리글을 읽어 자동 인식합니다.

### 계정 (자동 생성)

`이름 / 학년 / 반 / 역할(담임·관리자) / 상태(대기·승인·정지) / 비밀번호(암호화) / 신청일시 / 승인일시 / 최근접속 / 메모`

---

## 만들어진 기능

### 인증

- 비밀번호 하나로 로그인 → **본인 반이 바로 표시**
- 로그인 후에는 **모든 반 조회 가능** (담임·관리자 동일 권한)
- 비밀번호는 SHA-256 + salt 로 저장. 시트를 봐도 알 수 없음
- 접속증(토큰) 12시간. `PropertiesService` 에 보관
- 계정 신청 → 시트 `계정` 탭에 `대기` → 시트 상단 **🔐 상담시스템 관리** 메뉴에서 승인 시 비밀번호 자동 발급

### 화면 (왼쪽 메뉴 3개)

1. **우리반 학생 목록** — 학생 카드 (평균 등급, 희망 목표, 상담 횟수)
2. **개별 상담하기** — 내신 표, 모의고사 표, 상담 저장, 목표 저장, A4 1장 인쇄
   - 🎯 등급 상승 목표 (`상담_2차목표` 기반, 인쇄 포함)
   - 모의고사 등급·백분위 추이 그래프 (화면 전용, 인쇄 제외)
3. **통합 성적 관리**
   - 학년 성적 분석 리포트 — 과목별 통계(평균·표준편차·Q1·중앙값·Q3), 등급 분포, 반별 히트맵, 성취도 분포, 모의고사 평균 등급 추이
   - 성적 데이터 열람 — 전교생 표. 내신 9개 항목 / 모의고사 월별 점수·백분위·등급, 반 필터

### 9등급 환산

현행 5등급제 옆에 `2 → 3` 형태로 병기합니다. 시트 수식은 안 건드립니다.

```
내신성적 시트의 전교생 점수로 과목별 석차백분율을 다시 계산
  석차백분율 = (석차 + (동석차 - 1) ÷ 2) ÷ 인원수 × 100
9등급 기준: 4 / 11 / 23 / 40 / 60 / 77 / 89 / 96 %

기준 점수 — 1차등급: 1차시험 / 2차등급: 2차시험 / 최종등급: 환산총점
```

평균은 5등급제·9등급제 둘 다 표시. 단위수 가중치 없음(단순 평균).

---

## 코드 규약

### Code.gs

- **ES5로 작성** (`var`, `function`). 화살표 함수·템플릿 리터럴 쓰지 말 것
- 바깥에 열린 함수는 `doGet` / `doPost` 뿐. 모든 요청은 `handle_(req)` 를 거침
- 학생 데이터 함수는 이름을 `_` 로 끝냄 (`getStudentsByClass_` 등)
- 데이터 요청은 전부 `verifyToken_` 통과 필요
- **캐시(CacheService)를 쓰지 않습니다.** 시트를 고치면 즉시 반영되는 것이 사용자와의 약속입니다
- 전교생 통계는 서버에서 계산해 **숫자만** 내보냄 (원자료를 브라우저로 보내지 않음)

### index.html

- 단일 파일. Tailwind(cdn.tailwindcss.com) + Chart.js(cdnjs) 사용
- `api(action, payload)` → POST(text/plain) 실패 시 JSONP 예비통로로 재시도
- `.hidden { display:none !important }` 를 직접 정의해 둠 (CDN 지연 시 화면 노출 방지)
- 인쇄용 `@media print` 규칙 유지 — **A4 1장** 이 요구사항
- 로그인 화면의 **🔧 연결 상태 확인** 버튼이 진단 도구. `checkApiUrl()` 이 주소 형식을 검사
  (개인 계정 `/macros/s/` 와 학교 계정 `/a/macros/<도메인>/s/` 둘 다 통과해야 함)

---

## 작업 방식

수정 후에는 아래를 **직접 돌려서 확인한 뒤** 결과를 보고하세요. 추측으로 "될 겁니다" 하지 말 것.

```bash
# 1) 문법
cp Code.gs _c.js && node --check _c.js && rm _c.js
python3 -c "import re;h=open('index.html',encoding='utf-8').read();open('_h.js','w',encoding='utf-8').write('\n;\n'.join(re.findall(r'<script(?![^>]*src=)[^>]*>(.*?)</script>',h,re.S)).replace('const API_URL','var API_URL'))" && node --check _h.js && rm _h.js

# 2) 기능 — SpreadsheetApp·PropertiesService·Utilities 등을 흉내 낸 하네스에서
#    Code.gs 를 eval 하고 doPost 로 실제 요청을 넣어 검증
# 3) 화면 — playwright 로 google.script.run / fetch 를 가짜 응답으로 가로채고
#    로그인 → 화면 전환 → 표 렌더링까지 눌러본 뒤 pageerror 확인
```

- 사용자에게 보고할 때는 **무엇을 몇 개 검증했는지** 구체적으로 적을 것
- 확인 못 한 것은 "확인하지 못했다"고 분명히 말할 것
- 실수했으면 감추지 말고 먼저 밝힐 것

---

## 남은 일 / 주의

- [ ] `상담_2차목표` 실제 열 구조 확인 (`check상담목표시트` 로그 필요)
- [ ] 모의고사 9월·10월은 아직 미입력 상태
- [ ] 2학기 내신은 아직 미입력 상태
- 단위수 가중 평균은 미구현 (사용자가 원하면 시트의 단위수 열 위치 확인 필요)
- `내신_등급계산`, `상담_계산`, `내신_반영비율`, `내신_성취도분할점수` 탭은 아직 안 씀
